# YouTube Aggregator (Next.js)

## Setup

1. Install dependencies:
```bash
npm install
```

2. Start development server:
```bash
npm run dev
```

No YouTube API key is required. The app resolves channel IDs through a server route and embeds each channel's live stream endpoint.
